import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'rgba(249, 210, 72, 0.5)'
  },
  displayModeButton: {
    position: 'absolute',
    height: 50,
    width: 50,
    borderRadius: 25,
    backgroundColor: 'rgb(249, 210, 72)',
    bottom: 30,
    right: 30,
    justifyContent: 'center',
    alignItems: 'center'
  }
})

export default styles